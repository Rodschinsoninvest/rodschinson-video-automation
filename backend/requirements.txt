# Web stack — pinned to the versions the app actually runs on locally.
# (fastapi<0.135 ships an older Starlette whose request.form() lacks
# max_part_size → multipart uploads 413 with "Upload too large or malformed".)
fastapi==0.135.2
uvicorn[standard]==0.42.0
python-multipart==0.0.22
pydantic==2.12.5
httpx==0.28.1

# Config / IO
python-dotenv==1.2.2
aiofiles==25.1.0
anthropic==0.86.0
bcrypt==5.0.0          # password hashing

# Document + image processing (lazy-imported in main.py — must be installed)
pillow==12.1.1          # PIL — photo downscaling / de-dupe
pymupdf==1.27.2.2       # fitz — render PDF plans to images
pypdf==6.10.0           # extract text from PDF documents
python-docx==1.2.0      # extract text from Word documents
